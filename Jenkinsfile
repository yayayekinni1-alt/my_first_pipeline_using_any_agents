pipeline {
    agent agent {
                docker {
                    image 'node:26-alpine'
    stages {
        stage('NPM') {
            
                }
            }
            steps {
                sh 'npm --version'
                sh 'node --version'
            }
        }
    }
}
